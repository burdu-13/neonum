import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
    selector: 'app-nn-global-loader',
    imports: [],
    templateUrl: './nn-global-loader.html',
    styleUrl: './nn-global-loader.scss',
    changeDetection: ChangeDetectionStrategy.OnPush,
})
export class NnGlobalLoader {
  
}
