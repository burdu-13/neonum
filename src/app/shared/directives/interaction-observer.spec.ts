import { InteractionObserver } from './interaction-observer';

describe('InteractionObserver', () => {
  it('should create an instance', () => {
    const directive = new InteractionObserver();
    expect(directive).toBeTruthy();
  });
});
